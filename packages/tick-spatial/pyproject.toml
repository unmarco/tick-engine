[project]
name = "tick-spatial"
version = "0.1.0"
description = "Spatial indexing for the tick engine"
requires-python = ">=3.11"
dependencies = [
    "tick>=0.2.1",
]

[tool.uv.sources]
tick = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["tick_spatial"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
